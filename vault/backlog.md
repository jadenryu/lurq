# lurq agent backlog

The agent only picks up **unchecked** `- [ ]` items below. This is the
human-in-the-loop task source — the agent literally cannot work on anything you
haven't written here. Keep every item SMALL and verifiable (one PR each), and
check it off `- [x]` once merged (or to pause it).

- [ ] Add a unit test for the typosquat edit-distance threshold at the boundary (distance == 2)
- [ ] Handle an empty pgvector result set in the ranker without throwing
- [ ] Document the MCP `recommend` tool's input/output schema in the README
